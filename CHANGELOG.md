# Changelog

タグの注記から抜粋(各バージョン1行)。新しい順。

## v0.12.0
- **カードの状態を Claude Code の hooks から受け取る**ようにした(TUI 依存の縮小)。起動時に `--settings` でカード専用の hook 設定を注入し、`UserPromptSubmit`/`PreToolUse`/`PostToolUse`→稼働中、`Stop`→完了 を同梱 fleet-bridge が状態ファイルへ書き、Fleet が監視して反映する。実セッションで `UserPromptSubmit→working … Stop→idle` の遷移を確認済み。`--settings` はユーザー自身の設定と**マージ**され(同一イベントでも両方発火)、`~/.claude/settings.json` は一切変更しない。フックは stdout に何も出さず必ず 0 で終了する純観測(exit 2 はセッションの動作をブロックしてしまうため)
- **承認待ち(Blocked)は引き続き TUI 検知**。「このフォルダを信頼しますか」等のダイアログは hook が一切発火しないことを実測で確認したため、hook 状態が稼働中/完了でも画面が Blocked ならそちらを優先する
- hook コマンドにも `--root` を明示。渡さないと FLEET_ROOT 使用時に「アプリは FLEET_ROOT を監視、hook は ~/.fleet に書く」という無言の不一致になる

## v0.11.2
- **Claude が作業中でも Working にならない**のを修正。Claude Code 2.1.234 で稼働シグナルが同時に2つ変わっていた: OSC タイトルのスピナーが点字(⠋)から半円(◐◑◒◓)へ、フッタの `esc to interrupt` は文字列ごと削除(バイナリ内 0 件)。Fleet はその両方に依存していたため丸ごと判定不能になっていた。実機の TUI を pty で捕捉して実測し、タイトルは点字と半円の両方を受け付け、本文側は動詞リストではなく**形**(スピナー字形+動詞+`…`、`· Ns ·` / `↓N tokens`)で照合する下部3行のルールに置き換えた。Codex 側(点字)は変更なし

## v0.11.1
- **A2A のメッセージが送信されていなかったのを修正**。注入は本文の末尾に LF を付けていたが、LF は入力欄に改行を入れるだけで送信ではない(送信は CR)。裏起動が入って「勝手に作業が進む」経路が通ったことで、人が Enter を押さない限り止まることが表に出た。本文と CR を**別の書き込み**で送るようにした — 同一書き込みにすると長文では貼り付けと見なされ CR が本文に飲まれる(短文では通るので短文だけで確かめると誤る)。同じカードへの連続注入はカード毎に直列化して 2 通が 1 通に混ざらないようにした
- 委譲で作るカードの**危険モードスキップを作成元カードから引き継ぐ**ようにした。従来は常に無効で、承認なしで走っているカードから委譲すると子だけが裏起動直後に承認待ちで止まっていた。intent には載せない(Agent が intent ファイルを直接書けるため、載せると Agent 自身が権限を昇格できてしまう)

## v0.11.0
- 委譲で生えたカードが**人がターミナルを開かなくても自分で Agent を起動する**ようになった。対象はその適用で新しく作られたカードだけ(既存カードを起動ごとに焼かない)。Fleet を閉じている間に届いた委譲も起動時に同じ扱いで裏起動する。必要だったのは初期フレームを非ゼロにすること 1 点だけで、`.zero` では PTY の winsize が 0x0 になり TUI が描画できなかった
- 新しい作業ディレクトリでの「このフォルダを信頼しますか」を Blocked として検知。裏起動したカードは必ずここで止まるのに `unknown` のままで、盤面が「人間待ち」を示せなかった
- 端末バッファの検知を実測に合わせて2点修正。(1) 起動直後は画面がスクロールしておらず全画面ダイアログは画面**上部**に描かれるため、可視画面全体を見る領域を追加(既存ルールの下部窓は変更なし)。(2) SwiftTerm は書き込まれていないセルを NUL で返し語間が空白にならないため、照合前に制御文字を空白へ倒して連続空白を畳む

## v0.10.1
- 無所属カードからも委譲できるように修正。従来はチャンネルガードが全ツールを一律に弾き、チャンネルはカードを2枚手で結線しないと生まれないため、1枚目のカードは `fleet_create_card` すら呼べず「最初の委譲」が原理的に不可能だった。委譲 intent を `~/.fleet/delegations/` へ置き、適用時に作成元と結線する(カードを作ることがチャンネルを作ること)
- `fleet_create_card` に `agent`(claude|codex)と `model` を追加。「Codex のカードを立ててレビューさせて」が表現できるようになった
- 委譲 intent は rename で claim してから適用する(Fleet 二重起動や適用途中のクラッシュでカードが2枚生えるのを防ぐ)。作業ディレクトリの検証を bridge 経路だけでなく本体側にも置いた(Agent は intent ファイルを直接書けるため)
- 既知の制約: 委譲で生えたカードの Agent は、そのカードのターミナルを開くまで起動しない

## v0.10.0
- カード単位のモデル指定。カードにモデル名を書くとそのカードのエージェントがそのモデルで起動する(Claude は `--model`、Codex は `model=` の設定上書き)。固定の選択肢ではなく自由入力なので新モデルも Fleet の更新不要。空欄は CLI の既定で既存カードは不変
- fleet-bridge に `--root` を明示的に渡すよう修正。bridge は `FLEET_ROOT` を読まないため、本体と bridge が別々に既定値へフォールバックして偶然一致している状態だった
- `FLEET_ROOT` 指定時は SwiftData の盤面もその中に置く(検証ビルドを普段使いの Fleet と並べて動かせる)

## v0.9.3
- worktree 削除で、捨てられる差分(種類別一覧 + ファイルごとの diff)を確認したうえで強制削除できるルートを追加。`.serena/` のような未追跡ゴミ1つで削除が完全に塞がる問題への対処。ブランチ ref は残すのでコミットは失われず、使用中セッションがある worktree は強制でも削除しない

## v0.9.2
- DONE / BLOCKED の知らせ方をシステム通知から Dock(バウンス + 要対応件数バッジ + 音)へ変更。自己署名配布のアプリは Gatekeeper に rejected され、システム通知の許可が原理的に下りない(要 notarization)ため、v0.9.0/v0.9.1 では実際には何も届いていなかった

## v0.9.1
- 未読(DONE)ステータスをアプリ再起動後も保持(従来は起動時に全部既読へリセットされていた)

## v0.9.0
- DONE / BLOCKED をカード名付きの macOS 通知で知らせる。クリックでそのカードのターミナルへ。開いているカードでは鳴らない。設定で種別ごとにオフ可

## v0.8.1
- worktree のベース最新化で認証ヘルパーを無効化しないよう修正(HTTPS リモートで必ず fetch 失敗していた)
- fetch に 20 秒タイムアウト、ssh は BatchMode で fail-fast

## v0.8.0
- worktree のベースを origin から最新化して作成(古いベースからの派生を防止)。fetch 失敗/リモート無しは警告を出してフォールバック

## v0.7.3
- 使用中セッションのカードでも「セッションを終了して worktree も削除」を選べるように(従来は削除手段が無く詰んでいた)
- 終了済みシェルを「使用中」扱いしない

## v0.7.1
- A2A セキュリティ強化

## v0.7.0
- カード別 Claude プロファイル(CLAUDE_CONFIG_DIR)

## v0.6.4
- worktree 作成中の進捗表示

## v0.6.3
- worktree の git-lfs PATH 修正

## v0.6.2
- worktree セッション自動復帰の修正

## v0.6.1
- worktree 作成 UI の改善(現在ブランチ表示・トグル化・ブランチ一覧)

## v0.6.0
- Fleet-managed git worktrees
